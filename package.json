{
  "name": "workers-ocr-demo",
  "private": true,
  "workspaces": ["workers", "web"],
  "scripts": {
    "dev": "npm run dev -w workers",
    "dev:workers": "npm run dev -w workers",
    "dev:web": "npm run dev -w web",
    "deploy": "npm run deploy -w workers",
    "typecheck": "npm run typecheck -w workers && npm run typecheck -w web"
  }
}
